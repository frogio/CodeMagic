enum EduType { None, RepeatEdu, BasicEdu, Accumulated }

enum QuizType { Basic, WordSelect, MistakeNote, WORKBOOK }

enum EndQuizNavigation { GoHome, BackToCondition }

enum SocialLoginType { KAKAO, GOOGLE, APPLE }

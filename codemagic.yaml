workflows:
  ios-build:
    name: iOS Build
    environment:
      xcode: 15.3
    scripts:
      - name: Install Flutter dependencies
        script: |
          flutter --version
          flutter pub get
      - name: Build iOS
        script: |
          flutter build ios --release
    artifacts:
      - build/ios/ipa

workflows:
  ios-workflow:
    name: iOS Build
    environment:
      flutter: 3.19.0
      xcode: 15.3
  scripts:
  - name: Install Flutter dependencies
    script: |
      flutter pub get
  - name: Build iOS
    script: |
      flutter build ios --release
